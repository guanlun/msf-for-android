package hk.org.msf.android.ui;

/**
 * This class is not in use, because the videos are player using intent.
 */
/*
public class VideoPlayer extends Activity {

	@Override
	public void onCreate(Bundle savedInstanceState) {
		
		super.onCreate(savedInstanceState);
		this.setContentView(R.layout.video_player);
		
		VideoView vv = (VideoView)findViewById(R.id.video_view);
		
		vv.setVideoURI(Uri.parse("http://s.ytimg.com/yt/swfbin/watch_as3-vflwQAc_A.swf"));
		vv.setMediaController(new MediaController(this));
		vv.requestFocus();
		vv.showContextMenu();
		vv.start();
	}
}
*/